/* ==========================================
   WEURA AI — Vercel Serverless Handler (api/index.js)
   ========================================== */

const app = require('../app');
module.exports = app;
